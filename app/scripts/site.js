/**
 * @name Site
 * @description Global variables and functions
 * @version 1.0
 */
angular.module('my-app', ['ngMaterial']).config(['$compileProvider', function($compileProvider) {
  // $compileProvider.debugInfoEnabled(false);
}]);
